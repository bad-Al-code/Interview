/**
 * merge k sorted data structures, such as arrays and linked list, into a singe data structure that maintainst the
 * sorterd order.
 *
 * IDEA:
 * 	- repeateedly select the smallest (or largest dependeing on the sorting order) element among the k input lst and add
 * 	it to the output list.
 * 	- continue untill all elemnts from all input lists ave been added to the output list
 */
